module.exports.abc = {
    sqrt: function(x){

      return Math.sqrt(x);
    },
    squar:function (x) {
        return x * x;
    }
    // diag:function (x, y) {
    //     return Math.sqrt(square(x) + square(y));
    // } ,
}
