module.exports = {
    sqrt: function(x){
      return Math.sqrt(x);
    },
    square: function (x) {
        return x * x;
    }
  //  diag:function (x, y) {
      //  return Math.sqrt(square(x) + square(y));
  // } ,
}
